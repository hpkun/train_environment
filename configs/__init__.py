"""Experiment presets — short names for common training configurations.

This module is read by ``train_vanilla_mappo.py`` and ``train_attention_mappo.py``
via ``--preset <name>``.  CLI arguments override preset values.
"""