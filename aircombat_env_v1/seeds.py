"""Fixed, disjoint experiment seed sets."""
NOMINAL_VALIDATION_SEEDS = tuple(range(1000, 1050))
GENERALIZATION_LOW_SEEDS = tuple(range(2000, 2050))
GENERALIZATION_MEDIUM_SEEDS = tuple(range(3000, 3050))
GENERALIZATION_HIGH_SEEDS = tuple(range(4000, 4050))
HELDOUT_FINAL_SEEDS = tuple(range(5000, 5050))
